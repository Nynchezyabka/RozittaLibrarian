# Rozitta Librarian — зависимости
# Каркас: FastAPI + WebSocket на порту 8011 (как у RozittaTranscriber)
fastapi>=0.110
uvicorn[standard]>=0.27
websockets>=12.0
# БД — стандартный sqlite3 из Python, FTS5 встроен
# (никаких внешних ORM — deliberate, см. librarian_рабочий_план.md §2)
# Тесты
pytest>=8.0
# Замер качества поиска (scripts/eval_fts.py + eval_questions.yaml)
# Не нужен для работы Librarian — только для прогона эталонных замеров
pyyaml>=6.0
